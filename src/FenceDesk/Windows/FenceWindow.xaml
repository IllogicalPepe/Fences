<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="FenceDesk.Windows.FenceWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title=" ">
    <Grid x:Name="RootGrid" AllowDrop="True" DragOver="RootGrid_DragOver" Drop="RootGrid_Drop">
        <Border
            x:Name="Glass"
            BorderBrush="#28FFFFFF"
            BorderThickness="1"
            CornerRadius="8">
            <Border.Background>
                <SolidColorBrush x:Name="GlassBrush" Color="#B80F1724" />
            </Border.Background>
        </Border>

        <Grid Padding="8,6,8,8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid
                x:Name="TitleBar"
                Grid.Row="0"
                Padding="2,0,2,4"
                DoubleTapped="TitleBar_DoubleTapped"
                PointerPressed="TitleBar_PointerPressed">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock
                    x:Name="TitleText"
                    VerticalAlignment="Center"
                    FontSize="12"
                    FontWeight="SemiBold"
                    Foreground="#C8D0DC"
                    Text="Fence" />
                <Button
                    x:Name="RollButton"
                    Grid.Column="1"
                    Width="22"
                    Height="20"
                    Margin="4,0,0,0"
                    Padding="0"
                    Background="Transparent"
                    BorderThickness="0"
                    Click="RollButton_Click"
                    Content="▲"
                    FontSize="9"
                    Foreground="#8C96A5"
                    ToolTipService.ToolTip="Roll up / expand" />
            </Grid>

            <StackPanel
                x:Name="TabStrip"
                Grid.Row="1"
                Margin="0,0,0,4"
                Orientation="Horizontal"
                Visibility="Collapsed" />

            <Grid x:Name="Body" Grid.Row="2">
                <ScrollViewer
                    HorizontalScrollBarVisibility="Disabled"
                    VerticalScrollBarVisibility="Auto">
                    <GridView
                        x:Name="ItemsHost"
                        IsItemClickEnabled="False"
                        SelectionMode="None">
                        <GridView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <ItemsWrapGrid Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </GridView.ItemsPanel>
                        <GridView.ItemContainerStyle>
                            <Style TargetType="GridViewItem">
                                <Setter Property="Margin" Value="0" />
                                <Setter Property="Padding" Value="0" />
                                <Setter Property="MinWidth" Value="0" />
                                <Setter Property="MinHeight" Value="0" />
                                <Setter Property="Background" Value="Transparent" />
                            </Style>
                        </GridView.ItemContainerStyle>
                    </GridView>
                </ScrollViewer>
                <TextBlock
                    x:Name="HintText"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    FontSize="11"
                    Foreground="#8C96A5"
                    Opacity="0.85"
                    TextAlignment="Center"
                    Text="Drop files here&#x0a;Right-click for options" />
            </Grid>
        </Grid>
    </Grid>
</Window>
